"""Resolve which Sources to fetch in a given sync run.

Thin wrapper around src/rir2localdb/sources.py that filters by tier
configuration and (optionally) by --only/--skip CLI flags.
"""
# TODO(stage-1)
